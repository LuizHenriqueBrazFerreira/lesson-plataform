import HomeBackground from '../components/HomeBackground';

function Homepage() {
  return (
    <main>
      <HomeBackground>
        {' '}
      </HomeBackground>
    </main>
  );
}

export default Homepage;
